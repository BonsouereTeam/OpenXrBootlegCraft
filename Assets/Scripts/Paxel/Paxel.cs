using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Paxel : MonoBehaviour
{
    public Head head;
    public Shaft shaft;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void OnHitting(Collision collision)
    {

    }
}
